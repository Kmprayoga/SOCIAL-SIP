{
  "name": "backend-qrcode",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@hapi/hapi": "^21.3.2",
    "@google-cloud/firestore": "^6.1.0"
  }
}
